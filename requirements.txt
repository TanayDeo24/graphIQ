# Core data / numeric
pandas>=2.1,<3
numpy>=1.26,<2
scipy>=1.11,<2

# Modeling
scikit-learn>=1.3,<1.6
scikit-survival>=0.22,<0.24
lifelines>=0.27,<0.30
statsmodels>=0.14,<0.15
shap>=0.44,<0.47
torch>=2.2,<3

# Viz
matplotlib>=3.8,<4
seaborn>=0.13,<0.14

# API
fastapi>=0.110,<0.116
uvicorn[standard]>=0.29,<0.31
pydantic>=2.6,<3

# Explainability agent
tenacity>=8.0,<10
sqlglot>=30.0,<31
requests>=2.28,<3
# Local LLM backend (LLM_BACKEND=local) deps -- mlx, mlx-lm,
# sentence-transformers -- live in requirements-local.txt, not here.
# Apple Silicon only, and not needed for LLM_BACKEND=cloudflare (the
# deployed/production setting) -- see requirements-local.txt's own header
# comment and the README's Setup instructions for the two-file install.

# DB
sqlalchemy>=2.0,<2.1
psycopg2-binary>=2.9,<3

# Data acquisition / utils
kaggle>=2.2,<2.3
python-dotenv>=1.0,<2
tqdm>=4.66,<5
